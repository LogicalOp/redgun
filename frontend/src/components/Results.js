import React from 'react';
import { Panel, Card } from '@ui-webcomponents/react';


const Results = () => {
    return (
        <Panel
            accessibleRole="Form"
            headerLevel="H1"
            headerText="Panel"
            onToggle={function _a(){}}
        >
            Test
        </Panel>
    );
};

export default Results;