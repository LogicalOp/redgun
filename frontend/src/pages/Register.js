import React from 'react';
import { isAuthenticated } from '../utils/auth';

const Register = () => {
    
    return (
        <div>
            <h1>Register</h1>
        </div>
    );
};

export default Register;
